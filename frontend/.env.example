# Base URL of the deployed backend API (no trailing slash).
# Leave empty for local dev if using a Vite proxy; set to your Render backend URL in production.
VITE_API_BASE_URL=https://your-backend.onrender.com
